// com.example.conversa.model/Contact.kt
package com.example.conversa.model

data class Contact(
    val id: Int,
    val nome: String
    // Adicione outros campos do seu contato aqui, se houver (ex: val avatarUrl: String?)
)