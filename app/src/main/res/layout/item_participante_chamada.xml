<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:padding="16dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/ivAvatarParticipante"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/CircularImageView"
        app:tint="@color/corPrimaria"
        tools:src="@drawable/ic_person" />

    <TextView
        android:id="@+id/tvNomeParticipante"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:textSize="18sp"
        android:textColor="@color/corPrimaria"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/ivAvatarParticipante"
        app:layout_constraintEnd_toStartOf="@id/ivStatusParticipante"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="João Silva" />

    <TextView
        android:id="@+id/tvStatusParticipante"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:textSize="14sp"
        android:textColor="@color/corPrimaria"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/ivAvatarParticipante"
        app:layout_constraintEnd_toStartOf="@id/ivStatusParticipante"
        app:layout_constraintTop_toBottomOf="@id/tvNomeParticipante"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Conectado" />

    <ImageButton
        android:id="@+id/ivStatusParticipante"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/mutar_participante"
        android:src="@drawable/ic_volume_up"
        android:tint="@color/corPrimaria"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
